/* eslint-disable */
// Generated by scripts/generate-mui-stories.mjs — do not edit manually.
"use client";

import type { Meta, StoryObj } from "@storybook/react-vite";

import { BlockStory } from "../../shared/block-story";
import { useState } from "react";
import Box from "@mui/material/Box";
import Button from "@mui/material/Button";
import Collapse from "@mui/material/Collapse";
import Fade from "@mui/material/Fade";
import Grow from "@mui/material/Grow";
import Stack from "@mui/material/Stack";

const meta = {
  title: "MUI/Components/Transitions",
  tags: ["autodocs"],
  parameters: {
    docs: {
      description: {
        component: `Enter and exit animations (Collapse, Fade, Grow).`,
      },
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const CollapseFadeGrow: Story = {
  name: "Collapse, Fade, Grow",
  render: () => {
    function TransitionsDemo() {
      const [show, setShow] = useState(true);
      return (
        <Stack spacing={2}>
          <Button size="small" onClick={() => setShow((v) => !v)}>
            Toggle
          </Button>
          <Collapse in={show}>
            <Box sx={{ p: 2, bgcolor: "action.hover" }}>Collapse</Box>
          </Collapse>
          <Fade in={show}>
            <Box sx={{ p: 2, bgcolor: "action.hover" }}>Fade</Box>
          </Fade>
          <Grow in={show}>
            <Box sx={{ p: 2, bgcolor: "action.hover" }}>Grow</Box>
          </Grow>
        </Stack>
      );
    }
    return (
      <BlockStory>
        <TransitionsDemo />
      </BlockStory>
    );
  },
};
