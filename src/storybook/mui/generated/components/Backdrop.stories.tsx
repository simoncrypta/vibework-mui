/* eslint-disable */
// Generated by scripts/generate-mui-stories.mjs — do not edit manually.
"use client";

import type { Meta, StoryObj } from "@storybook/react-vite";

import { BlockStory } from "../../shared/block-story";
import { useState } from "react";
import Backdrop from "@mui/material/Backdrop";
import Button from "@mui/material/Button";
import CircularProgress from "@mui/material/CircularProgress";

const meta = {
  title: "MUI/Components/Backdrop",
  tags: ["autodocs"],
  parameters: {
    docs: {
      description: {
        component: `Focus-limiting overlay behind modals.`,
      },
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const Basic: Story = {
  name: "Basic",
  render: () => {
    function BackdropDemo() {
      const [open, setOpen] = useState(false);
      return (
        <>
          <Button onClick={() => setOpen(true)}>Show backdrop</Button>
          <Backdrop
            open={open}
            onClick={() => setOpen(false)}
            sx={{ color: "#fff", zIndex: (t) => t.zIndex.drawer + 1 }}
          >
            <CircularProgress color="inherit" />
          </Backdrop>
        </>
      );
    }
    return (
      <BlockStory>
        <BackdropDemo />
      </BlockStory>
    );
  },
};
