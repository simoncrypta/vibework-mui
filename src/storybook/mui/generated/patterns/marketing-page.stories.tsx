/* eslint-disable */
// Generated by scripts/generate-mui-stories.mjs — do not edit manually.

import type { Meta, StoryObj } from "@storybook/react-vite";

import Box from "@mui/material/Box";
import Button from "@mui/material/Button";
import Card from "@mui/material/Card";
import CardContent from "@mui/material/CardContent";
import Container from "@mui/material/Container";
import Grid from "@mui/material/Grid";
import Stack from "@mui/material/Stack";
import Typography from "@mui/material/Typography";

const meta = {
  title: "MUI/Patterns/Marketing/Marketing page",
  tags: ["autodocs"],
  parameters: {
    layout: "fullscreen",
    docs: {
      description: {
        component: "Hero + feature grid + CTA — MUI Marketing page template structure.",
      },
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const Page: Story = {
  render: () => (
    <Box>
      <Box sx={{ bgcolor: "primary.main", color: "primary.contrastText", py: 8 }}>
        <Container maxWidth="md">
          <Stack spacing={2} sx={{ alignItems: "center", textAlign: "center" }}>
            <Typography variant="h3" component="h1">
              Ship prototypes faster
            </Typography>
            <Typography variant="h6" sx={{ opacity: 0.9 }}>
              Figma to clickable UI with Material UI and Vibework.
            </Typography>
            <Stack direction="row" spacing={2}>
              <Button variant="contained" color="secondary" size="large">
                Get started
              </Button>
              <Button variant="outlined" color="inherit" size="large">
                View docs
              </Button>
            </Stack>
          </Stack>
        </Container>
      </Box>
      <Container maxWidth="lg" sx={{ py: 6 }}>
        <Grid container spacing={3}>
          {["Design tokens", "Component catalog", "Deploy anywhere"].map((title) => (
            <Grid key={title} size={{ xs: 12, md: 4 }}>
              <Card variant="outlined" sx={{ height: "100%" }}>
                <CardContent>
                  <Typography variant="h6" gutterBottom>
                    {title}
                  </Typography>
                  <Typography color="text.secondary">
                    Browse foundations and patterns in Storybook, then drop components into your
                    app.
                  </Typography>
                </CardContent>
              </Card>
            </Grid>
          ))}
        </Grid>
      </Container>
    </Box>
  ),
};
