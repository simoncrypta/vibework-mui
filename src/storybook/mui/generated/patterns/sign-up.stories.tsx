/* eslint-disable */
// Generated by scripts/generate-mui-stories.mjs — do not edit manually.
"use client";

import type { Meta, StoryObj } from "@storybook/react-vite";

import Box from "@mui/material/Box";
import Button from "@mui/material/Button";
import Link from "@mui/material/Link";
import Stack from "@mui/material/Stack";
import TextField from "@mui/material/TextField";
import Typography from "@mui/material/Typography";

const meta = {
  title: "MUI/Patterns/Auth/Sign up",
  tags: ["autodocs"],
  parameters: {
    layout: "fullscreen",
    docs: {
      description: {
        component: "Registration form with name and credentials — MUI Sign-up template layout.",
      },
    },
  },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

export const Page: Story = {
  render: () => (
    <Box
      sx={{
        minHeight: 480,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        p: 3,
      }}
    >
      <Stack spacing={2} sx={{ width: "100%", maxWidth: 400 }}>
        <Typography component="h1" variant="h4" sx={{ textAlign: "center" }}>
          Sign up
        </Typography>
        <TextField label="Full name" fullWidth autoComplete="name" />
        <TextField label="Email address" type="email" fullWidth autoComplete="email" />
        <TextField label="Password" type="password" fullWidth autoComplete="new-password" />
        <Button variant="contained" fullWidth size="large">
          Create account
        </Button>
        <Typography variant="body2" sx={{ textAlign: "center" }}>
          Already have an account? <Link href="#">Sign in</Link>
        </Typography>
      </Stack>
    </Box>
  ),
};
